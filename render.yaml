services:
  - type: web
    name: bellboy-whatsapp-bot
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: bellboy-data
      mountPath: /opt/render/project/src
      sizeGB: 1
